package com.group20;

/**
 * Game entity used to exit a level.
 */
public class Exit extends Entity{

    /**
     * Constructor for Exit entity
     * @param pos position for exit
     */
    Exit(Position pos){
        this.setPosition(pos);
    }
}
